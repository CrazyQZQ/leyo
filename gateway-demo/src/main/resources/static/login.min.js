$(function () {
    var a = $("#username"), b = $("#password");
    $("#login").on("click", function (c) {
        var d, e;
        return c.preventDefault(), d = a.val().trim(), e = b.val().trim(), "" === d ? (alert("用户名不能为空"), void 0) : "" === e ? (alert("密码不能为空"), void 0) : ($.post(ctx + "oauth/token", {
            username: d,
            password: e,
            grant_type: "password",
            client_id: "qq",
            client_secret: "111"
        }, function (a) {
            console.log("登录接口返回：", a);
        }).error(function (a) {
            console.error(a)
        }), void 0)
    })
});